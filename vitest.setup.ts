// Centralized test setup. Matchers / testing-library helpers are imported
// explicitly in each test file to keep the node<->jsdom boundary explicit and
// the install minimal. Kept as a placeholder so setupFiles wiring is trivial to
// extend (e.g. @testing-library/jest-dom) later.
export {};
